// No lead open — the slot renders nothing. Required: without a default,
// Next has no state to fall back to for the slot on a hard navigation.
export default function PanelDefault() {
  return null;
}
